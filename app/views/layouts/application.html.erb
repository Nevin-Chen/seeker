<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Seeker" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-white shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <%= link_to "Seeker", root_path, class: "flex items-center text-xl font-bold text-gray-900" %>
          </div>

          <% if Current.user %>
            <div class="flex items-center space-x-4">
              <span class="text-gray-700"><%= Current.user.username %></span>
              <%= button_to "Log out", session_path, method: :delete, class: "text-gray-700 hover:text-gray-900" %>
            </div>
          <% else %>
            <div class="flex items-center space-x-4">
              <%= link_to "Log in", login_path, class: "text-gray-700 hover:text-gray-900" %>
              <%= link_to "Sign up", signup_path, class: "bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700" %>
            </div>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <% if flash[:notice] %>
        <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded mb-4">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded mb-4">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
